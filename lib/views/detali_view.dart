import 'package:flutter/material.dart';

class DetaliView extends StatelessWidget {
  const DetaliView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DetaliView'),
      ),
    );
  }
}
